# Set default behavior for all files to use CRLF line endings on Windows
* text=auto eol=crlf

# Set specific file types to use LF (Unix) line endings
*.sh text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf

# Mark files as binary (no line ending conversion)
*.jpg binary
*.png binary
*.gif binary
*.pdf binary
*.zip binary
*.tar.gz binary

# Use Git LFS for large binary files
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.psd filter=lfs diff=lfs merge=lfs -text
